{
  "name": "humble-owned-overlay",
  "version": "3.4.0",
  "private": true,
  "description": "Release tooling for the Humble Bundle Owned Overlay Chrome extension.",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-extension.mjs",
    "build:zip": "node scripts/build-release.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
